// creating the backendUrl configuration
const backendUrlConfig = {
    url:'http://192.168.43.133:3000',
    secret:'I love PT'
  };

// exporting the backendUrl configuration
export default backendUrlConfig;
